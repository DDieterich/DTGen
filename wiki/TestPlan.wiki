#summary Test Plan

 = Functional Testing =
  # == Fundamental Database Features ==
    # _*DTC*_: Datatype Support for Columns
      # NUMBER
      # VARCHAR2
      # VARCHAR2(4001) conversion to CLOB
      # DATE
      # TIMESTAMP WITH TIME ZONE
      # TIMESTAMP WITH LOCAL TIME ZONE
    # _*NNCC:*_ Not NULL Constraints for Columns
    # _*CCCT:*_ Custom Check Constraints for Tables
      # Enforced Discrete Domains (no FK)
      # =, !=, ^=, <>, >, <, >=, <= (Equality/Inequality test)
      # ANY, SOME, ANY (List Comparisons)
      # NOT, AND, OR (Logical Conditions)
      # LIKE, REGEXP_LIKE (Pattern Matching)
      # IS NULL, IS NOT NULL (NULL Conditions)
      # (, ) (Compound Conditions)
      # BETWEEN, EXISTS, IN (Misc. Conditions)
    # *CIT*: Custom Indexes for Tables
      # UNIQUE
      # Non-UNIQUE
    # Comment Generation
      # *CGC*: Column Comments
      # *CGT*: Table Comments
      # *CGV*: View Comments
    # *TST*: Tablespace Storage for Tables
      # Application Default Tablespace for ACTIVE tables
      # Application Default Tablespace for History/Audit tables
      # Application Default Tablespace for ACTIVE indexes
      # Application Default Tablespace for History/Audit indexes
      # Entity Specific Tablespace for ACTIVE tables
      # Entity Specific Tablespace for History/Audit tables
      # Entity Specific Tablespace for ACTIVE indexes
      # Entity Specific Tablespace for History/Audit indexes
  # == Augmented Application Functionality ==
    # *NKUV*: Natural Key Update-able Views
      # INSERT with/without Primary Key
      # INSERT with/without EFF_START_DTM
      # INSERT with Foreign Key
        # Primary Key
        # Natural Key(s)
        # PK Full Path
        # NK Full Path
      # UPDATE with/without EFF_START_DTM
      # UPDATE with Foreign Key
        # Primary Key
        # Natural Key(s)
        # PK Full Path
        # NK Full Path
      # DELETE
    # *APIT*: Full Procedural APIs for each Table
      # INSERT
        # Column List
        # Table Record
        # View Record
      # INSERT with/without Primary Key
      # INSERT with/without EFF_START_DTM
      # INSERT with Foreign Key
        # Primary Key
        # Natural Key(s)
        # PK Full Path
        # NK Full Path
      # UPDATE
        # Column List
        # Table Record
        # View Record
      # UPDATE with/without EFF_START_DTM
      # UPDATE with nkdata_provided_in
        # TRUE
        # FALSE
        # NULL
      # UPDATE with Foreign Key
        # Primary Key
        # Natural Key(s)
        # PK Full Path
        # NK Full Path
      # DELETE with/without EFF_END_DTM
    # *DMLIT*: DML Integrity Enforcement for Tables
      # INSERT with/without Primary Key
      # INSERT with/without EFF_START_DTM
      # UPDATE with/without EFF_START_DTM
      # DELETE
    # *FPHC*: Full Path Hierarchical Data for Columns
      # Primary Key Full Path
      # Natural Key Full Path
    # *CFC*: Enforced Case Folding for Columns
      # UPPER
      # LOWER
      # INITCAP
    # *HAT*: Entity Based History and Audit for Tables
      # Optional Effectivity
      # Point-in-Time *_ASOF_* View
    # *POPT*: Audited *_POP_* Function for Tables
      # Pop Deleted Record
      # Replace an Active Record
      # Pop Active Record
  # == Augmented Schema Functionality ==
    # *AIT*: Automatic Indexes for Tables
      # Foreign Keys
      # Natural Keys
    # *PFT*: Calculated PCTFREE for Tables
      # Datatype storage size
      # Default PCTFREE for required columns
      # Default PCTFREE for non-required columns
  # == Cross-Cutting Concerns ==
    # Lock Helper for Global Single Threaded Logic
    # Debug Logging using Autonomous Transaction
    # Error Logging using Autonomous Transaction
    # Long Operations Tracking
    # Self-Reporting Version Number
  # == Source code Generation Specifics ==
    # Copyright in File Header
    # Always over-writes previous file
    # Database object compilation results reporting
  # == Default Maintenance User Interface ==
    # Data Domain Filterable Grid Edit
    # Generated PL/SQL for CLOB DML
    # Comprehensive *_OMNI_* View Forms
    # GUI Query-able Reporting
      # *_ASOF_*
      # *_OMNI_*

 = Global Parameter Options =
|| *Test Set* || *DB Integrity* || *Case Correction* || *Ignore No Change* ||
|| A || No || No || No ||
|| B || Yes || No || No ||
|| C || No || Yes || No ||
|| D || Yes || Yes || No ||
|| E || No || No || Yes ||
|| F || Yes || No || Yes ||
|| G || No || Yes || Yes ||
|| H || Yes || Yes || Yes ||

*Note:* "NK_SEP" and "PATH_SEP" parameters are not tested.

 = Deployment Configurations =
|| DB Account Names          || *DB Schema* || *DB User* || *MT Schema* || *MT User* || *DOD MT Schema* || *DOD MT User* ||
|| *Triggers/Constraints*    || TDBST       || TDBUT     || TMTST       || TMTUT     || TMTSTDOD        || TMTUTDOD      ||
|| *No Triggers/Constraints* || TDBSN       || TDBUN     || TMTSN       || TMTUN     || TMTSNDOD        || TMTUNDOD      ||

*Note:* Multiple Oracle Owners/Schema are created in a single database.  The special database link "loopback" is used to simulate the connection between the database and the mid-tier.

|| Accessed Via    || *DB User* || *MT Schema* || *MT User* || *DOD MT Schema* || *DOD MT User* ||
|| *DB Schema*     || Synonym   || DB Link     ||           ||                 ||               ||
|| *MT Schema*     ||           ||             || Synonym   ||                 ||               ||
|| *DB User*       ||           ||             ||           || DB Link         ||               ||
|| *DOD MT Schema* ||           ||             ||           ||                 || Synonym       ||

 = Boundary Condition Testing =
|| *Condition Name* || *Below* || *Lowest* || *Highest* || *Above* || *Nullable* ||
|| Number of Tables || 0 || 1 || 200 || 201 || No ||
|| Number of Columns in a Table || 0 || 1 || 99 || 100 || No ||
|| Number of Natural Key Columns || 0 || 1 || 99 || 100 || No ||
|| NUMBER Datatype Length || 0 || 1 || 39 || 40 || Yes ||
|| NUMBER Datatype Scale || -85 || -84 || 127 || 128 || Yes ||
|| VARCHAR2 Datatype Length || 0 || 1 || 32767 || 32768 || No ||
|| TIMESTAMP WITH TIME ZONE Datatype Length || -1 || 0 || 9 || 10 || Yes ||
|| TIMESTAMP WITH LOCAL TIME ZONE Datatype Length || -1 || 0 || 9 || 10 || Yes ||
|| Exception Code || -21000 || -20999 || -20000 || -19999 || No ||