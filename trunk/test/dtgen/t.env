
if [ ${#} -ne 2 -o ${1} != "sys" ]
then
   echo "Use the form: $[0] sys password"
fi
GENABBR=DTGEN
OWNERNAME=dtgen_test
OWNERPASS=dtgen_test
logfile=`basename ${0} .sh`.log
echo "logging to ${logfile}"
